<Window x:Class="EventsWPF_Practic.ParticipantsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Участники" Height="400" Width="600">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListView x:Name="ParticipantsListView" Margin="0,0,0,10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Имя" DisplayMemberBinding="{Binding FirstName}" />
                    <GridViewColumn Header="Фамилия" DisplayMemberBinding="{Binding Surname}" />
                    <GridViewColumn Header="Email" DisplayMemberBinding="{Binding EmailAddress}" />
                </GridView>
            </ListView.View>
        </ListView>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Content="Добавить" Click="AddParticipantButton_Click" Width="100" Margin="0,0,10,0"/>
            <Button Content="Удалить" Click="DeleteParticipantButton_Click" Width="100"/>
        </StackPanel>
    </Grid>
</Window>